import { FilterNewPipe } from './filter-new.pipe';

describe('FilterNewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNewPipe();
    expect(pipe).toBeTruthy();
  });
});
